import { formatPrompt } from "@/lib/utils"
import { ButtonCard } from "./button-card"

type SearchResultCardProps = any;

export async function SearchResultCard({ data, alwaysShowDownloadBtn }: SearchResultCardProps) {
  return <h1>sup</h1>;
}
